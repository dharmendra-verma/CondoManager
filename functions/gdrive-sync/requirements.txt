# Runtime dependencies for the gdrive-sync Azure Function (CM-34).
#
# The deployment bundles the repo's `agents/` package alongside this app
# (see .funcignore), so this file must cover everything the imported
# `agents.knowledge` + `agents.observability` modules need at runtime.
# Version ranges mirror the repo's pyproject.toml so dev and deploy agree.
#
# Azure Functions Python worker
azure-functions>=1.20,<2

# agents.knowledge — data models, chunking, embedding, Drive, Cosmos
pydantic>=2.7,<3
langchain-text-splitters>=0.3,<1
langchain-openai>=0.2,<1
azure-cosmos>=4.7,<5
azure-identity>=1.15,<2

# agents.knowledge.gdrive_client — Google Drive service-account access
google-api-python-client>=2.100,<3
google-auth>=2.35,<3

# agents.observability — structured logging + OTel pipeline (imported by the
# trigger's configure_logging / with_request_id helpers)
opentelemetry-api>=1.27,<2
opentelemetry-sdk>=1.27,<2
opentelemetry-exporter-otlp-proto-http>=1.27,<2
opentelemetry-instrumentation-fastapi>=0.48b0,<1
opentelemetry-instrumentation-httpx>=0.48b0,<1
openinference-instrumentation-openai>=0.1,<1
traceloop-sdk>=0.30,<1
azure-monitor-opentelemetry>=1.6,<2
